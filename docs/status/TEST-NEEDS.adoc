// SPDX-License-Identifier: CC-BY-SA-4.0
= TEST-NEEDS: bofj-kitt

== CRG Grade: X — Untested (as of 2026-07-16)

bofj-kitt is a pre-alpha scaffold. It has no product code and therefore no
product tests yet. This file states, honestly, what exists today and what the
roadmap requires before a real grade can be claimed.

== Current State (2026-07-16)

[cols="2,1,4", options="header"]
|===
| Category | Count | Details

| *Source modules* | 0 | No implementation language chosen yet (see ROADMAP M3)
| *Unit tests* | 0 | None — no product code to exercise
| *Integration tests* | 0 | None
| *E2E tests* | 0 | Removed the template-instantiation harness; real e2e returns at M3
| *Workflow tests* | 1 | `tests/workflows/validate_workflows_test.sh` (governance workflows present + well-formed)
| *Aspect checks* | 1 | `tests/aspect_tests.sh` (repo-shape/observability/security aspect scaffold)
| *Fuzz tests* | 0 | `tests/fuzz/README.adoc` scaffold only; no harness
|===

The only tests that run today validate the repository's own governance shape,
not any product behaviour.

== What's needed (tied to the roadmap)

See `docs/status/ROADMAP.adoc` and
`docs/reports/audit/repo-assessment-2026-07-16.adoc`.

=== Before CRG D (M3–M4)

* [ ] Choose the kit's implementation language (M3; from the allowed set in
  `.machine_readable/anchors/ANCHOR.a2ml`).
* [ ] Land the first shippable kit capability (e.g. `kitt init` provisioning a
  compliant dev environment; `kitt connect` registering with a BoJ server).
* [ ] Per-capability unit + integration tests, with counts recorded in
  `.machine_readable/6a2/STATE.a2ml`.
* [ ] Re-add a real `.github/workflows/e2e.yml` exercising the capability
  end-to-end (the template placeholder was removed in the 2026-07-16 pass).
* [ ] Replace the placeholder fuzz README with an actual harness once there is
  a parser/input surface to fuzz.

=== Before CRG C (M5)

* [ ] Active dogfooding on estate repos with no known home-context failures
  across an evidence window; populate `STATE.a2ml [dogfooding-status]`.
* [ ] Replace the generic template proofs under `verification/` with proofs
  about this project's actual behaviour (see `docs/status/PROOF-NEEDS.adoc`).

== Priority: P1 — blocked on product definition (M2)

Testing cannot meaningfully begin until the kit's first capability is specified
(M2) and a language is chosen (M3). Until then the grade is honestly *X*.
