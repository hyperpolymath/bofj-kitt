;; SPDX-License-Identifier: MPL-2.0
;; Project state — update throughout each session
(state
  (metadata
    (version "1.1.0")
    (project "bofj-kitt")
    (last-updated "2026-07-16"))
  (project-context
    (description "Shared Developer Environment Extension Kit (the SuperChainsaw) for the BoJ (Box of Justice) server")
    (primary-language "not yet chosen — see docs/status/ROADMAP.adoc M3")
    (status "pre-alpha"))
  (current-position
    (phase "design")
    (completion-percentage 5)
    (milestone "M0 — truthful identity and green governance CI"))
  (testing-summary
    (product-tests "none — no product code yet (pre-alpha scaffold)")
    (workflow-tests "tests/workflows/validate_workflows_test.sh: governance workflows present + well-formed")
    (aspect-checks "tests/aspect_tests.sh: repo-shape/observability/security scaffold"))
  (critical-next-actions
    ("M1 — finish remaining placeholder-token fills; restore a full SECURITY policy"
     "M2 — specify the kit's first capability + BoJ integration contract"
     "M3 — choose implementation language; re-add a real e2e workflow")))
